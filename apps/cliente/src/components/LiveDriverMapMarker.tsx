/**
 * Alias do marcador ao vivo (ETA) — usado nas telas de viagem em andamento.
 * Implementação: {@link DriverEtaMarkerIcon}.
 */
export { DriverEtaMarkerIcon as LiveDriverMapMarker } from './DriverEtaMarkerIcon';
