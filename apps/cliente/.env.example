# Este arquivo é mantido apenas como referência. Para rodar o app,
# configure o `.env` na RAIZ do monorepo (usa `npm run sync-env` para
# espelhar automaticamente para este app).
#
# Veja a lista completa em ../../.env.example

EXPO_PUBLIC_SUPABASE_URL=
EXPO_PUBLIC_SUPABASE_ANON_KEY=
EXPO_PUBLIC_MAPBOX_ACCESS_TOKEN=
EXPO_PUBLIC_GOOGLE_MAPS_API_KEY=
EXPO_PUBLIC_STRIPE_PUBLISHABLE_KEY=
EXPO_PUBLIC_APP_SCHEME=take-me-cliente
